# OpenSageTV Vibe TMDB tasks

This is the only active TMDB backlog. Completed work is removed and recorded
in `CHANGELOG.md` and `HANDOFF.md`.

- [ ] Activate the staged component update on isolated `.232`, restart the
  SageTV JVM, register/enable the standard plugin, and verify startup, live
  lookup, shared SageMC/XMLTV use, offline restart, and guarded rollback.
- [ ] After isolated physical commissioning, publish the versioned GitHub
  release and submit the generated V9 manifest to the OpenSageTV plugin repo.
